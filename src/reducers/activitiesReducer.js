const initialState = {
    activities: null,
    current_activity: null
}

export const layoutReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}